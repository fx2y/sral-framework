{
  "name": "@sral/generator",
  "version": "0.1.0",
  "description": "Stateless Cloudflare Worker for generating artifacts via LLM",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": ["cloudflare", "worker", "llm", "generation"],
  "author": "SRAL Framework",
  "license": "MIT",
  "dependencies": {
    "@sral/shared": "*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.1.0",
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2",
    "wrangler": "^3.28.2"
  }
}